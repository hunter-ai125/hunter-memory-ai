# AI 项目依赖
# ============================================================
# 核心依赖
torch>=2.0.0
# 向量检索（可选，但推荐）
sentence-transformers>=2.2.0
# HTTP 客户端（用于调用大模型API）
requests>=2.28.0

# ============================================================
# 可选依赖
# ============================================================
# 用于更快的向量检索（如果使用 chromadb）
# chromadb>=0.4.0

# 用于更丰富的关键词提取
# jieba>=0.42.1  # 中文分词